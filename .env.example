# For GitHub Pages project sites (hosted under /<repo>/).
# Example: https://github.com/<user>/<repo>
#
# If you leave this unset, Vite uses the default `./` which is fine for many setups.
VITE_BASE_URL=./

